CD490 — Counterfactuals & Alternative Technical Realities

Local preview
-------------

This is a single-file static site. To preview locally, run a simple HTTP server from the `CD490` folder.

Using Python 3:

```bash
cd /Users/bikki/Desktop/CD490
python3 -m http.server 8000
# then open http://localhost:8000 in your browser
```

Notes
-----
- The authoritative source for all factual text (names, dates, quoted lines, and exact form field values) is `final.pdf` in this folder. The site was built to reflect that document — if you need wording changes, update `final.pdf` or tell me which lines to copy exactly.
- The interactive Motion Economy Index slider simulates the compliance threshold (0.82) and triggers the on-form rejection stamp.

If you want, I can:
- extract precise verbatim text from `final.pdf` and update the site copy to match exactly;
- add printable artifact assets (PNG/PDF) and a mocked archival viewer.
# QWERTY-Game
